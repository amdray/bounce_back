

  

Created 8 todos

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/docs/CASE_MECHANICS_SUMMARY.md)

Searched for regex `switch.*i5|case.*i5` (`**/original_code/**/a.java`), no results

Starting: *Read a.java vertical collision cases* (2/8)

Now let me read the a.java file. It's likely large, so I'll start with understanding its structure and then read the relevant sections. 

Ran terminal command: Get-Content "d:\OneDrive\VS Code Project\psp\bounce_back\original_code\bounce_back_s60.jar.src\a.java" | Measure-Object -Line

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#1-1), lines 1 to 200

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#200-200), lines 200 to 400

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#400-400), lines 400 to 600

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#600-600), lines 600 to 800

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#800-800), lines 800 to 1000

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#1000-1000), lines 1000 to 1200

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#1200-1200), lines 1200 to 1400

Read [](file:///d%3A/OneDrive/VS%20Code%20Project/psp/bounce_back/original_code/bounce_back_s60.jar.src/a.java#1400-1400), lines 1400 to 1676

Now I have all the data. Let me compile the complete analysis. 

Starting: *Compile results report* (8/8)

---

## Результаты верификации CASE_MECHANICS_SUMMARY.md

### 1. MISSING CASES (отсутствуют в таблице)

| Case | Что делает (по коду) | Где в a.java |
|-----:|---|---|
| **7** | Разрушаемый блок. При `F=true` → вызов `a(row,col)` (замена тайла на 105 + анимация взрыва). При `F=false` → обычное столкновение со стеной | Верт: a.java, Гориз: a.java |
| **8** | То же, что case 7 (разрушаемый блок) | Там же, `(i5 == 7 \|\| i5 == 8) && this.F` |
| **79** | Squeeze-тайл: выталкивает игрока ВЛЕВО (`D--`). Если заблокирован и `!F` — проверяет соседний тайл на летальность → возможно `k()` | a.java |
| **82** | Squeeze-тайл, группа с 79/109 (выталкивание влево) | Там же |
| **83** | Squeeze-тайл: выталкивает игрока ВПРАВО (`D++`). Если заблокирован и `!F` — летальная проверка → возможно `k()` | a.java |
| **84** | Squeeze-тайл, группа с 83/108 (выталкивание вправо) | Там же |
| **108** | Squeeze-тайл, группа с 83/84 (выталкивание вправо) | Там же |
| **109** | Squeeze-тайл, группа с 79/82 (выталкивание влево) | Там же |

**Также не документированы field-тайлы** (не в switch, а в if/else перед коллизиями):
- Вертикальная сила: тайлы **45, 51, 53, 67, 71, 75** — изменяют `j` на ±100 (или ±50 для 75), a.java
- Горизонтальная сила влево: тайлы **44, 50, 76** — торможение + сила −125/−250, a.java
- Горизонтальная сила вправо: тайлы **43, 49, 52, 54, 66, 69, 73** — сила +250, a.java

---

### 2. ОШИБКИ В ОПИСАНИЯХ

| Case | Колонка | Ошибка | Правильно (по коду) |
|-----:|---|---|---|
| **11** | Влияние на состояние | Написано «не в смерти/анимации» | Условие: `I=true && F=false && a==0`. `F` — это щит, не смерть. Также пропущен вызов `c()` перед `l()`: код делает `c(); l(); o.b(4);` ([строка 535](original_code/bounce_back_s60.jar.src/a.java#L535)) |
| **12** | Влияние на очки | Не упомянуты звук и `E.C` | Пропущены: `o.b(2)` (звук 2) и `E.C += 1000` (счётчик уровня). a.java |
| **12** | Влияние на объект | «Обновляет флаг перерисовки E.M» без уточнения | `E.M = true` устанавливается ТОЛЬКО когда `h < 5`, а не всегда. a.java |
| **15** | Влияние на состояние | Пропущено `x=false` | Код: `this.x = false;` ([строка 514](original_code/bounce_back_s60.jar.src/a.java#L514)). Также пропущен условный звук: `if (!p) o.b(6)` |
| **18** | Влияние на состояние | Пропущен вызов `c()` | Код: `c(); l(); o.b(3);` — `c()` (сброс) вызывается первой. a.java |
| **22** | Влияние на состояние | «c(); b();» не пояснены | Вызываются `c()` (без параметров, reset) и `b()` (без параметров, shield trigger) — это НЕ `c(tile)` / `b(tile)`. Эти функции не описаны в Примечаниях |
| **26** | Влияние на состояние | Пропущен звук | `if (!C) o.b(6)` — звук 6 при первой активации. a.java |
| **30** | Влияние на очки | Не упомянут `E.C` | Пропущено: `E.C += 2500`. a.java |
| **34** | Влияние на очки | Не упомянут `E.C` | Пропущено: `E.C += 200`. a.java |
| **34** | Влияние на объект | Описание неполное | Пропущена деталь: предыдущий чекпоинт проверяется по условию `(b2 & 0x7F) == 35`, и `35` заменяется на 0 (с сохранением бита 0x80). Новый записывается как `0x23` (=35). a.java |
| **39** | Влияние на состояние | Пропущен звук | `if (!j) o.b(6)` — звук 6 при первой активации. a.java |
| **52** | Физика | Описан только как наклонная | В горизонтальных коллизиях case 52 обрабатывается в группе 52/53/54/55 (попытка обхода по Y), а не как slope. a.java |
| **200** | Физика | Слишком упрощено | Не различает: (a) вертикально-движущиеся платформы (`b12!=0`); (b) горизонтально-движущиеся (`b11!=0`); (c) разные размеры по `ao` (ao==2: 11×24, ao==0: 32×32, ao==1: 16×16). a.java |

---

### 3. ПРОПУЩЕННЫЕ МЕХАНИКИ

1. **Разрушение тайлов 7/8**: При `F=true` → `a(row, col)` → тайл заменяется на 105, ставится анимация (E.B/E.v/E.J). a.java

2. **Squeeze-механика (тайлы 79/82/83/84/108/109)**: При вертикальном столкновении выталкивают игрока по X. Если выталкивание заблокировано, проверяется соседний тайл — если летальный (85-89/63/64/92) и `!F`, вызывается `k()`. a.java

3. **Выход из уровня**: Когда `E.o=true` (все кольца собраны) и игрок внутри зоны выхода (`E.P < D < E.P+32`, `E.R < i < E.R+48`) → `E.ap=true`, звук 7, вибрация 1. a.java

4. **Поля скорости (field tiles)**: Тайлы 43-54/66-76 влияют на скорость вне collision switch (см. раздел 1 выше).

5. **Gravity stepping при горизонтальном движении**: Когда `x=true` (stоит на земле) и движется по X, игрок автоматически проваливается на 1px вниз (или вверх при `p`), если есть пустота. a.java

6. **Power-up timer → HUD**: Таймеры `B` и `b` управляют `E.ae = timer/18` (отображение полоски в HUD). При отскоке `E.ae` от текущего → `E.M = true` (перерисовка). a.java

7. **Тайл 105 как «взрыв» (результат разрушения 7/8)**: функция `a(row,col)` ставит `R[row][col]=105`, добавляет анимационный слот с длительностью 15 тиков.

---

### 4. ПРОПУЩЕННЫЕ ФУНКЦИИ И ПЕРЕМЕННЫЕ

**Функции без описания в Примечаниях:**

| Функция | Назначение | Строка |
|---|---|---|
| `l()` | Запуск перехода I-формы: если `!q` → `a = I ? 2 : 1`, `q=true`, `A=3` | a.java |
| `b()` (0 params) | Запуск перехода щита: если `!q` → `r = I ? 3 : 1`, `q=true`, `A=3` | a.java |
| `c()` (0 params) | Сброс анимации: если `!F && a==0 && r==0` → `A=0`, `q=false`, спрайт→11(I) или 0 | a.java |
| `g()` | Покадровая анимация щита (r=1→активация, r=2→деактивация, r=3→снятие I+щит) | a.java |
| `a()` (0 params) | Покадровая анимация I-формы (a=1→активация, a=2→деактивация) | a.java |
| `j()` | Покадровая анимация спрайта (g+1 / g-1 / reset) | a.java |
| `h()` | Расчёт скорости прыжка: −125 (норм), −180 (I), −95 (F); модификаторы C и l | a.java |
| `f()` | Расчёт скорости отскока: −83 (норм), −120 (I), −63 (F); модификатор C | a.java |
| `a(row, col)` 2 params | Разрушение тайла: `R[row][col]=105`, создание анимации в `E.B[]/E.v[]/E.J[]` | a.java |
| `e()` | Респаун: `o.h--`, сброс позиции на чекпоинт `H*16+8 / n*16+8`, сброс всех флагов | a.java |
| `i()` | Очистка ввода: `m &= 0xFFFFFFF0` | a.java |

**Переменные без документации:**

| Переменная | Тип | Назначение |
|---|---|---|
| `E.C` | int | Счётчик очков уровня (инкрементируется параллельно с `o.n` для cases 12/30/34) |
| `E.W` | int | Оставшиеся кольца (упомянут в таблице, но не в Примечаниях) |
| `E.o` | boolean | Флаг «выход открыт» (все кольца собраны) |
| `E.P`, `E.R` | int | Позиция двери выхода (X, Y) |
| `E.ap` | boolean | Флаг «уровень пройден» |
| `E.ae` | int | Значение полоски таймера в HUD (вычисляется как `B/18` или `b/18`) |
| `o.n` | int | Глобальный счёт игрока |
| `o.h` | int | Количество жизней |
| `m` | int | Битовая маска ввода (биты 1=left, 2=right, 4=down, 8=up) |
| `k` | int | Индекс последнего столкновения с движущимся объектом (−1 = нет) |
| `bool2` | local | Флаг «игрок стоит на field-тайле» — модифицирует поведение slopes в коллизиях |

**Звуки и вибрация (полная карта):**

| ID | Вызов | Событие | Документировано? |
|---|---|---|---|
| Звук 0 | `o.b(0)` | Смерть (`k()`) | Да (в примечаниях) |
| Звук 1 | `o.b(1)` | Кольцо собрано (hoops) | Частично (в таблице) |
| Звук 2 | `o.b(2)` | Коллектибл/чекпоинт (cases 12/30/34) | Только для 30 и 34 |
| Звук 3 | `o.b(3)` | Переход в I-форму (case 18) | Да |
| Звук 4 | `o.b(4)` | Переход из I-формы (case 11) | Да |
| Звук 5 | `o.b(5)` | Активация щита F (case 22, !F) | Да |
| Звук 6 | `o.b(6)` | Режим p/C/j (cases 15/26/39, условный) | **Нет** ❌ |
| Звук 7 | `o.b(7)` | Выход из уровня | **Нет** ❌ |
| Вибрация 0 | `o.c(0)` | Смерть | Да |
| Вибрация 1 | `o.c(1)` | Выход из уровня | **Нет** ❌ |

---

### Итого

- **8 missing cases**: 7, 8, 79, 82, 83, 84, 108, 109
- **12 ошибок/неточностей** в описаниях (cases 11, 12, 15, 18, 22, 26, 30, 34, 39, 52, 200)
- **7 пропущенных механик** (разрушаемые блоки, squeeze, выход, field-тайлы, gravity step, HUD-таймер, тайл 105)
- **11 недокументированных функций**, **11 недокументированных переменных**
- **3 пропущенных звука/вибрации** (звук 6, звук 7, вибрация 1) 

Completed: *Compile results report* (8/8)